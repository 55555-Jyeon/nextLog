type TParams = {
  params: {
    slug: string;
  };
};
type Metadata = {
  title: string;
  desc: string;
};
type TPostProps = {
  title: string;
  summary: string;
  image: string;
  slug: string;
  date: string;
};
