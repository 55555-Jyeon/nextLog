export type TParams = {
  params: {
    slug: string;
  };
};
export type Metadata = {
  title: string;
  desc: string;
};
export type TPostProps = {
  title: string;
  summary: string;
  image: string;
  slug: string;
  date: string;
};
